
<header class="header">
      <div class="nav__right">
          <%= link_to "新規班作成", new_team_path, class: :nav__btn %>
      </div>

<% @teams.each do |team| %>
  <%= link_to team.team_name, edit_team_path(team.id), class: :nav__btn %>
  <%= team.affiliation %>
  <%= team.work %>
  <%end %>